import * as React from 'react';
// import { HogeState } from '../../states/hogeState';
// import { HogeActions } from '../../containers/hogeContainer';

interface OwnProps {}

// type EnemyProps = OwnProps & HogeState & HogeActions;

// export const Help: React.SFC<any> = (props: EnemyProps) => {
export const Help: React.SFC<any> = (props: any) => {
  return <div>Help</div>;
};
